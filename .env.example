# URL base do apex20-backend (ConnectRPC / HTTP)
# Em desenvolvimento local, aponta para o container via Docker Compose
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL base do apex20-ws (WebSocket)
# Usado pelo hook de conexão WS (Sprint 2)
NEXT_PUBLIC_WS_URL=ws://localhost:8081
